# Secrets — never commit
.env
# Note: dbt/profiles.yml IS committed — it contains only env_var() references,
# no literal secrets (credentials come from the environment / GitHub secrets).

# Python
__pycache__/
*.pyc
.venv/
venv/
env/
.ipynb_checkpoints/

# dbt
dbt/logs/
dbt/target/
dbt/dbt_packages/
logs/

# OS / editor / local tooling
.DS_Store
.idea/
.vscode/
.claude/
dbt/.user.yml

# Data dumps
*.csv
*.parquet
*.duckdb
